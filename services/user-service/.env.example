# =============================================================================
# User Service (Go) - Environment Variables
# =============================================================================
# cp .env.example .env 후 값을 수정하세요.
# Docker 환경에서는 docker-compose.yml의 environment로 주입됩니다.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
ENV=dev                          # dev, prod
SERVER_PORT=8081                 # 서버 포트
LOG_LEVEL=debug                  # debug, info, warn, error

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# DATABASE_URL 형식 (권장)
DATABASE_URL=postgresql://user_service:user_service_password@localhost:5432/wealist_user_db?sslmode=disable

# 또는 개별 변수 사용
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=user_service
# DB_PASSWORD=user_service_password
# DB_NAME=wealist_user_db

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# 64자 이상의 안전한 시크릿 키 사용 권장
SECRET_KEY=your-jwt-secret-key-at-least-64-bytes-for-hs512-algorithm

# -----------------------------------------------------------------------------
# Service URLs
# -----------------------------------------------------------------------------
AUTH_SERVICE_URL=http://localhost:8080    # auth-service URL (토큰 검증용)

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://localhost:8080

# -----------------------------------------------------------------------------
# S3/MinIO Configuration (파일 업로드용)
# -----------------------------------------------------------------------------
S3_BUCKET=wealist-local-files
S3_REGION=ap-northeast-2
S3_ENDPOINT=http://localhost:9000         # MinIO 사용 시
S3_ACCESS_KEY=minioadmin                  # MinIO 사용 시
S3_SECRET_KEY=minioadmin                  # MinIO 사용 시
