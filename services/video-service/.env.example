# =============================================================================
# Video Service - Environment Configuration
# =============================================================================
# 이 파일을 복사하여 .env 로 저장하고 필요한 값을 설정하세요.
# cp .env.example .env
# =============================================================================

# =============================================================================
# Server Configuration
# =============================================================================
ENV=dev
PORT=8004
SERVER_BASE_PATH=/api/video
LOG_LEVEL=debug

# =============================================================================
# Database Configuration (PostgreSQL)
# =============================================================================
# Docker Compose 환경에서는 docker-compose.yml의 환경변수를 사용합니다.
DATABASE_URL=postgresql://video_service:video_service_password@localhost:5432/wealist_video_db?sslmode=disable

# 개별 환경변수 (docker-compose에서 조합용)
VIDEO_DB_NAME=wealist_video_db
VIDEO_DB_USER=video_service
VIDEO_DB_PASSWORD=video_service_password

# =============================================================================
# Redis Configuration
# =============================================================================
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=redis_password

# =============================================================================
# Auth Service Configuration
# =============================================================================
AUTH_SERVICE_URL=http://localhost:8080
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-min-32-chars

# =============================================================================
# User Service Configuration
# =============================================================================
# Note: Include the full base path (e.g., /api/users) as the workspace endpoints are relative to this
USER_SERVICE_URL=http://localhost:8081/api/users

# =============================================================================
# CORS Configuration
# =============================================================================
# 모든 서비스 포트 포함: frontend(3000), board(8000), chat(8001), noti(8002),
# storage(8003), video(8004), auth(8080), user(8081), livekit(7880)
CORS_ORIGINS=http://localhost:3000,http://localhost:7880,http://localhost:8000,http://localhost:8001,http://localhost:8002,http://localhost:8003,http://localhost:8004,http://localhost:8080,http://localhost:8081,http://127.0.0.1:3000,http://127.0.0.1:8080

# =============================================================================
# LiveKit Configuration (WebRTC SFU Server)
# =============================================================================
# LiveKit 서버 HTTP API 주소 (토큰 생성 등)
LIVEKIT_HOST=http://localhost:7880

# LiveKit API 키 (livekit.yaml의 keys 설정과 일치해야 함)
LIVEKIT_API_KEY=devkey
LIVEKIT_API_SECRET=secret

# LiveKit WebSocket URL (클라이언트가 연결할 주소)
LIVEKIT_WS_URL=ws://localhost:7880

# =============================================================================
# Notes
# =============================================================================
# 1. 개발 환경에서는 Docker Compose를 통해 실행하는 것을 권장합니다.
# 2. LiveKit 설정은 docker/livekit/livekit.yaml의 keys 설정과 일치해야 합니다.
# 3. 프로덕션 환경에서는 반드시 SECRET_KEY와 LIVEKIT_API_SECRET을 변경하세요.
# =============================================================================
