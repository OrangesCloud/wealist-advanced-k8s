server:
  port: 8002
  env: dev
  log_level: debug

database:
  url: postgresql://noti_service:noti_service_password@localhost:5432/wealist_noti_db?sslmode=disable

redis:
  host: localhost
  port: 6379
  password: ""
  db: 0

auth:
  service_url: http://localhost:8080
  internal_api_key: internal-secret-key
  secret_key: your-jwt-secret-key

app:
  cache_unread_ttl: 300  # 5 minutes
  cleanup_days: 30
