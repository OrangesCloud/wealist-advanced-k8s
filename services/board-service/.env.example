# =============================================================================
# Board Service (Go) - Environment Variables
# =============================================================================
# cp .env.example .env 후 값을 수정하세요.
# Docker 환경에서는 docker-compose.yml의 environment로 주입됩니다.
# =============================================================================

# -----------------------------------------------------------------------------
# Server Configuration
# -----------------------------------------------------------------------------
ENV=dev                          # dev, prod
BOARD_SERVER_PORT=8000           # 서버 포트
LOG_LEVEL=debug                  # debug, info, warn, error
BOARD_USE_AUTO_MIGRATE=true      # DB 자동 마이그레이션

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# DATABASE_URL 형식 (권장)
DATABASE_URL=postgresql://board_service:board_password@localhost:5432/wealist_board_db?sslmode=disable

# 또는 개별 변수 사용
# DB_HOST=localhost
# DB_PORT=5432
# DB_USER=board_service
# DB_PASSWORD=board_password
# DB_NAME=wealist_board_db

# -----------------------------------------------------------------------------
# Redis Configuration (캐싱)
# -----------------------------------------------------------------------------
REDIS_URL=redis://:your-redis-password@localhost:6379/1
REDIS_PASSWORD=your-redis-password

# -----------------------------------------------------------------------------
# JWT Configuration
# -----------------------------------------------------------------------------
# 64자 이상의 안전한 시크릿 키 사용 권장
SECRET_KEY=your-jwt-secret-key-at-least-64-bytes-for-hs512-algorithm

# -----------------------------------------------------------------------------
# Service URLs
# -----------------------------------------------------------------------------
USER_SERVICE_URL=http://localhost:8080     # user-service URL
AUTH_SERVICE_URL=http://localhost:8090     # auth-service URL (토큰 검증용)
NOTI_SERVICE_URL=http://localhost:8002     # notification-service URL

# -----------------------------------------------------------------------------
# Internal API Configuration
# -----------------------------------------------------------------------------
# 서비스 간 통신용 API 키 (noti-service 호출 시 사용)
INTERNAL_API_KEY=your-internal-api-key

# -----------------------------------------------------------------------------
# CORS Configuration
# -----------------------------------------------------------------------------
CORS_ORIGINS=http://localhost:3000,http://localhost:8000,http://localhost:8080

# -----------------------------------------------------------------------------
# S3/MinIO Configuration (파일 업로드용)
# -----------------------------------------------------------------------------
S3_BUCKET=wealist-dev-files
S3_REGION=ap-northeast-2
S3_ENDPOINT=http://localhost:9000         # MinIO 사용 시
S3_ACCESS_KEY=minioadmin                  # MinIO 사용 시
S3_SECRET_KEY=minioadmin                  # MinIO 사용 시
