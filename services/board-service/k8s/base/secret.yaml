apiVersion: v1
kind: Secret
metadata:
  name: board-service-secret
type: Opaque
stringData:
  # Database
  DB_USER: "board_service"
  DB_PASSWORD: "changeme"
  # JWT (user-service와 동일한 값 사용)
  JWT_SECRET: "changeme-jwt-secret-key"
  # S3/MinIO 자격증명 (Local에서 MinIO용, Prod에서는 IAM Role 사용)
  # Local overlay에서 오버라이드됨
  S3_ACCESS_KEY: ""
  S3_SECRET_KEY: ""
