apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - deployment.yaml
  - service.yaml
  - configmap.yaml
  - secret.yaml
  # ingress.yaml - 통합 ingress로 이동 (infrastructure/base/ingress/)

labels:
  - pairs:
      app.kubernetes.io/name: board-service
      app.kubernetes.io/part-of: wealist
